version: '3.8'

services:
  smart-paddy:
    container_name: smart-paddy-app
    build: 
      context: .
      # ถ้าต้องใช้ env ตั้งแต่ตอน Build (เช่น NEXT_PUBLIC_...)
      args:
        - NEXT_PUBLIC_API_BASE=${NEXT_PUBLIC_API_BASE}
    restart: always
    ports:
      - "3000:3000"
    # 🔥 เพิ่มบรรทัดนี้เพื่อให้ Container ดึงค่าจากไฟล์ .env มาใช้ตอน Runtime
    env_file:
      - .env
    environment:
      - NODE_ENV=production
